# API 密钥必须通过环境变量传入，禁止写在代码或 config 中。
# 复制本文件为 .env 后填入实际值（不要提交 .env 到版本库）。

# GitHub（可选，用于 API 限流）
GITHUB_TOKEN=

# 日报生成 LLM（按 config.report.provider 选用其一）
MOONSHOT_API_KEY=
DASHSCOPE_API_KEY=
DEEPSEEK_API_KEY=
QIANFAN_API_KEY=

# Twitter/X API（可选，用于采集视频推文，mode=api 时使用）
TWITTER_BEARER_TOKEN=

# Twikit 登录信息（可选，用于非官方网页采集，mode=twikit 时使用）
# 建议使用单独的采集专用账号，并确保本文件只作为示例，真实值写在 .env 中且不要提交到仓库。
# 基本登录信息（至少需要 auth_info_1 与 password）：
TWIKIT_AUTH_INFO_1=      # 必填：用户名 / 邮箱 / 手机号（三选一，其一即可）
TWIKIT_AUTH_INFO_2=      # 可选：若登录流程要求再次输入邮箱/手机号，则在此填写
TWIKIT_PASSWORD=         # 必填：账号密码

# 如账号开启了 TOTP 双因素验证，可选填此项（base32 秘钥，用于自动生成验证码）：
TWIKIT_TOTP_SECRET=
